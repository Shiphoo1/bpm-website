import styles from "./Registration.module.css";
const Registration = (props) => {
  return (
    <div className={`${props.classNameReg} ${styles.button}`}>Qeydiyyat</div>
  );
};
export default Registration;
